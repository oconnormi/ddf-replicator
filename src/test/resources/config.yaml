ddf1:
  system:
    sysprop1: val1
    sysprop2: val2
  configs:
    etc/org.ddf.some.file1:
      prop1: val1
      prop2: val2
    etc/org.codice.ddf.ui.searchui.filter.RedirectServlet.config:
      defaultUri: "foo/bar"
    anotherFile:
      someprop: aVal
  applications:
    security-services-app: ""
    foo-app: ""
  includes:
    - src: path/to/keystore.jks
      dest: etc/keystores/fooStore.jks
    - src: path/to/truststore.jks
      dest: etc/keystores/serverTruststore.jks
ddf2:
  configs:
    fooFile:
      barProp: barVal
